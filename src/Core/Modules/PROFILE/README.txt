This module provides a means of handling a bundle of settings, called
a "profile". You can use these profiles to store things like a "raidbot" profile,
color schemes or just the whole bot configuration before trying out something new.